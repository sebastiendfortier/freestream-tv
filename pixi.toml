[workspace]
name = "freestream-tv"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = "3.12.*"
polars = ">=1.0"

[tasks]
sync-catalog = "python tools/sync_catalog_parquet.py"
verify-parquet = "python scripts/verify_parquet_catalog.py"
